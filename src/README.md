# Source

Authored Gloss source code will live here. This includes design tokens, theme
styles, component styles, and JavaScript behavior as those parts of the project
are introduced.

The Sass entrypoint is `styles/gloss.scss`. It compiles UIkit from the installed
dependency and is where generated runtime tokens and UIkit overrides meet.
