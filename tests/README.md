# Tests

Automated tests for Gloss will live here.

Component coverage should start from UIkit's upstream test fixtures and be
adapted only where Gloss changes the public class names or behavior. Gloss
specific regression tests belong alongside those fixtures rather than in
duplicated ad hoc preview pages.
